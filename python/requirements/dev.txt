# Dev/CI tooling, pinned so CI is reproducible and a new release of any of these
# can't silently break an unrelated PR. Dependabot bumps them (pip entry in
# dependabot.yml). Installed by dev.py (lint/test/build).

# build
build==1.5.0

# dev
ruff==0.16.3
pytest==9.1.1

# publish
twine==7.0.0
